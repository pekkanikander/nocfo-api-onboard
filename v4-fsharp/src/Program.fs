module Program

open System

[<EntryPoint>]
let main argv =
    printfn "TODO: Implement minimal demo"
    printfn "1. Read NOCFO_API_TOKEN env var"
    printfn "2. Create NocfoClient with HttpClient"
    printfn "3. Call getAccountsStream from Streams.fs"
    printfn "4. Display accounts as they stream in"

    0
