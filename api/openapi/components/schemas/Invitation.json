{
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "readOnly": true
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "readOnly": true,
      "description": "The date and time when the record was created."
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "readOnly": true,
      "description": "The date and time when the record was last updated."
    },
    "email": {
      "type": "string",
      "format": "email",
      "maxLength": 254
    },
    "role": {
      "$ref": "./RoleEnum.json"
    },
    "status": {
      "$ref": "./InvitationStatusEnum.json"
    }
  },
  "required": [
    "created_at",
    "email",
    "id",
    "updated_at"
  ]
}